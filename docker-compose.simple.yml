version: "3.8"

services:
  ministatus:
    image: rilmay/ministatus:latest  # Or specific version tag
    container_name: ministatus
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
    env_file:
      - .env
    restart: unless-stopped 